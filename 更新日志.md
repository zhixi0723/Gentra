# Gentra 更新日志

本文件记录 Gentra 每个版本的更新内容。应用每次更新后，会在「设置 → 关于 → 检查更新」处展示最新版本的更新说明。

---

## v1.2.0 — 2026-07-10

### 🆕 新增功能

- **剪贴板 · 图片记录**：自动记录复制的图片，支持缩略图预览、原图查看、一键复制回
- **剪贴板 · 暂停持久化**：暂停记录的状态在重启应用后保持，不再恢复为启用
- **存储 · 大文件扫描**：支持大小区间筛选（最小 ~ 最大 + MB/GB 单位），不再只有下限
- **存储 · 大文件扫描**：新增结果搜索框，可按文件名或路径即时过滤
- **存储 · 大文件 / 重复文件**：扫描中可点「停止」按钮中止，并释放扫描占用的内存
- **存储 · 重复文件**：采用 SHA-256 内容哈希比对，精确识别内容相同的文件
- **存储 · 文件工具**：批量重命名（前缀 + 序号 + 实时预览）+ 按类型分类整理（自动建子文件夹移动）
- **设置 · 通用**：开机自启、最小化到系统托盘、关闭时最小化、主题模式（浅色/深色/跟随系统）真实生效
- **设置 · 剪贴板联动**：启用记录、最大保存数量、记录图片、退出时清空 均实时控制主进程行为
- **设置 · 关于**：数据导出（JSON 备份）、检查更新、查看文档、提交反馈
- **系统托盘**：最小化/关闭隐藏到托盘，托盘图标 + 右键菜单（显示窗口 / 退出）
- **工作台**：首页汇总真实的剪贴板、备忘录、磁盘空间、下载目录数据

### ✨ 优化

- 全新 UI 风格（玻璃拟态 + 浅色主题）
- 应用图标替换为正式版本（托盘、窗口、安装包图标统一）
- 大文件表格固定列宽，超长文件名/路径省略号截断，鼠标悬停查看完整内容
- 切换标签页时自动停止当前扫描，避免后台偷偷占用内存与磁盘 IO

### 🐛 问题修复

- 修复暂停记录后恢复时，把暂停期间复制的内容补记进来的问题
- 修复托盘菜单「退出」无法退出（关闭最小化拦截了退出动作）
- 修复备忘录编辑时每按一个字符就存盘、且无法取消的问题（改为编辑/保存/取消模式）
- 修复设置页「暂停记录」与剪贴板页状态不同步的问题
- 修复开关按钮白色圆点不在水平线上的对齐问题
- 修复文件工具页「确认重命名」与「确认整理」按钮不在同一水平线的问题

---

## v1.1.0 — 2026-07-08

### ✨ 优化

- 重构整体 UI 组件体系（新增 PageHeader / Segmented / 卡片样式等基础组件）
- 应用由 CleanDesk 更名为 **Gentra**
- 适配新的应用图标

### 🐛 问题修复

- 修复主进程因编码问题导致 `require('electron')` 被注释、启动崩溃的问题
- 补回备忘录丢失的「从剪贴板生成」「搜索」「5 个筛选 Tab」「恢复按钮」等功能
- 补回设置页丢失的「检查更新」按钮
- 补回大文件扫描丢失的「最大值」区间输入

---

## v1.0.0 — 2026-07-05

### 🆕 初始版本

- **备忘录**：增删改查、置顶 / 完成 / 归档、搜索、5 个筛选 Tab、编辑模式、从剪贴板生成
- **剪贴板**：自动监听系统剪贴板（文本/链接/代码自动识别）、复制回、收藏、删除、清空、暂停/恢复、搜索
- **存储与文件**：磁盘概览、大文件扫描、下载目录、重复文件、文件工具（批量重命名 / 分类整理）
- **设置**：通用 / 剪贴板 / 存储 / 备忘 / 关于 五个分区，配置本地持久化
- **工作台**：首页汇总最近剪贴板、待处理备忘、磁盘空间、下载目录提醒
- **地基**：Electron 主进程 IPC + 本地 JSON 持久化 + React Context 数据层，全程零额外依赖

---

> 版本号规则：主版本.次版本.修订号。功能新增递增次版本，问题修复递增修订号。
